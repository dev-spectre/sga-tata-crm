---
phase: 07-backend-lead-branch-update-activity-audit-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: true
requirements:
  - BRCH-05
  - BRCH-06
must_haves:
  - truths:
    - "Branch updates persist in DB and return in lead response"
    - "BRANCH_CHANGE activity is logged for regular staff user"
    - "Consultant clearance correctly updates assignedConsultant to null"
    - "Zero activity logs are recorded when Superadmin executes branch update"
---

<objective>
Verify backend branch updates, consultant clearance, standard user audit logging (`BRANCH_CHANGE`), and Superadmin log suppression through an automated integration test script.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Execute Automated Verification Test for Branch Updates & Audit Logging</name>
  <files>scratch/verify-branch-pipeline.ts</files>
  <read_first>src/lib/activity.ts, src/app/api/leads/[id]/route.ts</read_first>
  <action>
    Create and execute an automated test script at `scratch/verify-branch-pipeline.ts` using `npx tsx`:
    1. Create a test lead in PostgreSQL with `branch: "Coimbatore"` and `assignedConsultant: "Test Consultant"`.
    2. Create a mock regular user session (`role: "USER"`, `username: "test_staff"`).
    3. Simulate lead update:
       - Update `branch` to `"Salem"` and `clearConsultant: true`.
       - Invoke `logLeadDiff`.
       - Verify DB lead now has `branch: "Salem"` and `assignedConsultant: null`.
       - Verify `LeadActivity` has entries for `BRANCH_CHANGE` (old: `"Coimbatore"`, new: `"Salem"`) and `CONSULTANT_ASSIGN` (old: `"Test Consultant"`, new: null).
    4. Simulate Superadmin lead update:
       - Update `branch` to `"Tirupur"` with Superadmin user session (`role: "SUPERADMIN"`, `username: "sudo"`).
       - Invoke `logLeadDiff`.
       - Verify DB lead has `branch: "Tirupur"`.
       - Verify NO new `LeadActivity` records were created for this update.
    5. Clean up test data and exit with 0 on success.
  </action>
  <verify>npx tsx scratch/verify-branch-pipeline.ts</verify>
  <acceptance_criteria>
    - Verification script runs to completion and exits with code 0
    - Confirms `branch` updates persist
    - Confirms `BRANCH_CHANGE` activity logged for regular user
    - Confirms Superadmin activity is completely suppressed
  </acceptance_criteria>
  <done>Backend branch update pipeline and Superadmin audit privacy verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Automated verification script passes with 0 errors
</verification>
