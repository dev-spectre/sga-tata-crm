---
phase: 04-geocoding-fallback-service-location-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/prisma.ts
autonomous: true
requirements:
  - LOC-03
must_haves:
  truths:
    - "Prisma schema defines LocationCache model with unique searchTerm, coordinates, and metadata"
    - "Database migration/push creates the LocationCache table in PostgreSQL"
    - "Prisma client exports locationCache types and operations on prisma.locationCache"
  artifacts:
    - prisma/schema.prisma
    - src/lib/prisma.ts
  key_links:
    - "prisma/schema.prisma defines model LocationCache"
    - "src/lib/prisma.ts exports extended client supporting locationCache operations"
---

<objective>
Define the persistent `LocationCache` data model in `prisma/schema.prisma`, synchronize the PostgreSQL database, and generate updated Prisma Client types.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-geocoding-fallback-service-location-cache/04-CONTEXT.md
@.planning/phases/04-geocoding-fallback-service-location-cache/04-RESEARCH.md

@prisma/schema.prisma
@src/lib/prisma.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LocationCache Model to Prisma Schema</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
    Update `prisma/schema.prisma` to append the `LocationCache` model:
    ```prisma
    model LocationCache {
      id            Int      @id @default(autoincrement())
      searchTerm    String   @unique
      canonicalName String
      district      String   @default("")
      state         String   @default("")
      latitude      Float
      longitude     Float
      source        String   @default("geocoder")
      createdAt     DateTime @default(now())
      updatedAt     DateTime @updatedAt

      @@index([searchTerm])
      @@index([canonicalName])
      @@index([state])
    }
    ```
  </action>
  <verify>npx prisma validate</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model LocationCache`
    - `searchTerm` is defined with `@unique`
    - `npx prisma validate` exits with code 0
  </acceptance_criteria>
  <done>LocationCache model added and validated in Prisma schema.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize Database and Update Prisma Client</name>
  <files>src/lib/prisma.ts</files>
  <read_first>src/lib/prisma.ts</read_first>
  <action>
    1. Run `npx prisma db push` to synchronize the `LocationCache` table to PostgreSQL non-destructively.
    2. Run `npx prisma generate` to generate updated client types.
    3. Update `src/lib/prisma.ts` to ensure `ExtendedPrismaClient` interface includes `locationCache: any;`.
  </action>
  <verify>npx prisma generate && npx tsc --noEmit</verify>
  <acceptance_criteria>
    - PostgreSQL contains `LocationCache` table
    - `src/lib/prisma.ts` exports `locationCache`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Database synchronized and Prisma client types compiled.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` returns valid schema
- [ ] `npx prisma generate` runs cleanly
- [ ] `npx tsc --noEmit` exits with 0 errors
</verification>
