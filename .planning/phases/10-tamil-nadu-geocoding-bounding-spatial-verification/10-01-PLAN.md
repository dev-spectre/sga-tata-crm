# Phase 10 Plan 01: Tamil Nadu Geocoding Bounding & Spatial Verification

**Phase:** 10-tamil-nadu-geocoding-bounding-spatial-verification
**Plan:** 10-01
**Status:** Ready to Execute
**Requirements Addressed:** GEO-05, GEO-06

## Objectives
1. Add Tamil Nadu geographic bounding box constants to `src/lib/location/geocoder.ts`.
2. Update `queryNominatim` to pass `viewbox` and `bounded=1` restricting results to Tamil Nadu bounds.
3. Update `queryGoogleGeocode` to pass `bounds` and `components=administrative_area:Tamil Nadu|country:IN`.
4. Implement `isWithinTamilNaduBounds(lat, lon)` helper and integrate into `resolveLocationTiered` and cache evaluation.
5. Ensure type safety with `npx tsc --noEmit`.

## Implementation Tasks
1. Edit `src/lib/location/geocoder.ts`:
   - Export `TAMIL_NADU_BOUNDS = { minLat: 8.08, maxLat: 13.55, minLon: 76.23, maxLon: 80.35 }`.
   - Export `isWithinTamilNaduBounds(lat: number, lon: number): boolean`.
   - Update `queryNominatim` and `queryGoogleGeocode` URL parameters with bounding coordinates.
   - Combine state name check and coordinate bounding check when computing `isTamilNadu`.
2. Verify TypeScript build cleanly.
