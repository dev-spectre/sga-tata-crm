---
phase: 09-superadmin-activity-log-integration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/activity/page.tsx
autonomous: true
requirements:
  - BRCH-06
must_haves:
  - truths:
    - "BRANCH_CHANGE action has distinct badge in /activity"
    - "BRANCH_CHANGE has human-readable description in /activity"
    - "Action filter dropdown includes Branch Changes"
    - "Superadmin actions are strictly suppressed from logs"
  - artifacts:
    - src/app/activity/page.tsx
  - key_links:
    - "src/app/activity/page.tsx formats BRANCH_CHANGE from LeadActivity"
---

<objective>
Enhance the Superadmin audit log dashboard (`src/app/activity/page.tsx`) with dedicated badge formatting, descriptive text generation, and action filtering for `BRANCH_CHANGE` events, and run end-to-end verification.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add BRANCH_CHANGE Formatting & Filter to Activity Page</name>
  <files>src/app/activity/page.tsx</files>
  <read_first>src/app/activity/page.tsx</read_first>
  <action>
    Modify `src/app/activity/page.tsx`:
    1. In `getActionBadge`:
       ```ts
       case "BRANCH_CHANGE":
         return { label: "Branch Changed", color: "#059669", bg: "rgba(5, 150, 105, 0.1)" };
       ```
    2. In `getActionDescription`:
       ```ts
       case "BRANCH_CHANGE":
         return `Changed branch from "${oldValue || 'Unassigned'}" to "${newValue || 'Unassigned'}"`;
       ```
    3. In action filter `<select>` (around line 1320):
       Add:
       ```tsx
       <option value="BRANCH_CHANGE">Branch Changes</option>
       ```
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/app/activity/page.tsx` contains `BRANCH_CHANGE` in badge, description, and select options
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Activity page updated with BRANCH_CHANGE display support.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-End Verification of Branch Override & Superadmin Log Invisibility</name>
  <files>scratch/verify-e2e-milestone.ts</files>
  <read_first>src/lib/activity.ts, src/app/api/admin/activity/logs/route.ts</read_first>
  <action>
    Create a verification script to validate:
    1. Create lead with initial branch.
    2. Standard staff user updates branch via API / activity pipeline.
    3. Query `/api/admin/activity/logs` logic: confirms the `BRANCH_CHANGE` record exists and includes old/new branch data.
    4. Superadmin updates lead branch.
    5. Confirms zero logs added for Superadmin.
    6. Confirm cleanup.
  </action>
  <verify>npx -y tsx --env-file=.env scratch/verify-e2e-milestone.ts</verify>
  <acceptance_criteria>
    - End-to-end verification passes
    - Confirms activity logs reflect staff branch changes
    - Confirms Superadmin activities are 100% invisible
  </acceptance_criteria>
  <done>End-to-end milestone verification complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` returns 0 errors
- [ ] Automated verification script passes with 0 errors
</verification>
