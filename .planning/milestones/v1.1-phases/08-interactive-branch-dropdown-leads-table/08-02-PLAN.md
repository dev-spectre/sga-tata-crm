---
phase: 08-interactive-branch-dropdown-leads-table
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/app/dashboard/page.tsx
autonomous: true
requirements:
  - BRCH-03
must_haves:
  - truths:
    - "Mobile card view features interactive branch dropdown"
    - "Mobile branch dropdown uses handleBranchChange with same consultant confirmation flow"
    - "Mobile layout is responsive and unclipped"
  - artifacts:
    - src/app/dashboard/page.tsx
  - key_links:
    - "src/app/dashboard/page.tsx renders branch dropdown in mobile card view"
---

<objective>
Equip the mobile card view in `src/app/dashboard/page.tsx` with the interactive branch dropdown, connected to the same consultant clearance confirmation workflow and optimistic state pipeline.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Interactive Branch Dropdown to Mobile Lead Cards</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx</read_first>
  <action>
    In `src/app/dashboard/page.tsx` (around line 2320 where mobile metadata is rendered):
    Replace the static branch tag in the mobile card item:
    ```tsx
    <div className="lead-mobile-meta-item">
      <span className="lead-mobile-meta-label">Branch</span>
      <select
        className={`branch-select ${!lead.branch ? 'branch-unassigned' : ''}`}
        style={{ width: '100%', maxWidth: '100%' }}
        value={lead.branch || ''}
        onChange={(e) => handleBranchChange(lead, e.target.value)}
        disabled={isLeadLocked}
        title={isLeadLocked ? `Locked by ${lead.handledBy}` : undefined}
      >
        <option value="">Unassigned</option>
        {branches.map((b) => (
          <option key={b} value={b}>
            {b}
          </option>
        ))}
      </select>
    </div>
    ```
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Mobile card view renders `<select>` for branch
    - Uses same `handleBranchChange` handler
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Mobile card view interactive branch dropdown wired up.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` returns 0 errors
</verification>
