---
phase: 05-nearest-branch-routing-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - src/lib/location/routing.ts
  - test/routing.test.mjs
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
must_haves:
  - truths:
    - "Routing decisions and distances are audited in LeadActivity"
    - "Automated tests verify distance accuracy across geographic benchmarks"
    - "Automated tests verify nearest branch selection, inactive branch skipping, and out-of-state fence"
  - artifacts:
    - test/routing.test.mjs
  - key_links:
    - "src/lib/location/routing.ts records audit entries via prisma.leadActivity"
---

<objective>
Implement routing activity audit logging (`logRoutingActivity`) and the comprehensive automated test suite (`test/routing.test.mjs`) verifying all distance, routing, and audit requirements.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add LeadActivity Audit Logging for Branch Routing</name>
  <files>src/lib/location/routing.ts</files>
  <read_first>src/lib/activity.ts, src/lib/location/routing.ts</read_first>
  <action>
    Enhance `src/lib/location/routing.ts`:
    1. Implement `logRoutingActivity(leadId: number, result: RoutingResult, username = 'System'): Promise<void>`:
       - If `result.status === 'assigned'` and `result.assignedBranch`:
         - action: `'AUTO_ASSIGN_BRANCH'`
         - newValue: `Assigned to ${result.assignedBranch.name} (${result.distanceKm} km away via ${result.resolvedLocation?.source || 'location'})`
       - If `result.status === 'out_of_state'`:
         - action: `'ROUTING_OUT_OF_STATE'`
         - newValue: `Lead location (${result.resolvedLocation?.state || 'outside TN'}) is out of state - unassigned`
       - If `result.status === 'unresolved'`:
         - action: `'ROUTING_UNRESOLVED'`
         - newValue: `Could not resolve lead location "${result.resolvedLocation?.query || ''}" - unassigned`
       - Create entry in `prisma.leadActivity`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `logRoutingActivity` function exported from `src/lib/location/routing.ts`
    - Adheres to `prisma.leadActivity` schema
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Audit logging for branch routing implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build Comprehensive Routing Test Suite</name>
  <files>test/routing.test.mjs</files>
  <read_first>src/lib/location/routing.ts</read_first>
  <action>
    Create `test/routing.test.mjs`:
    1. Test Haversine distance accuracy:
       - Distance from a point to itself is 0.
       - Coimbatore to Tiruppur is ~43-45 km.
       - Chennai to Madurai is ~415-425 km.
    2. Test Nearest Branch Selection with mock branches:
       - Peelamedu routes to Coimbatore Branch.
       - Avadi routes to Chennai Branch.
       - Dindigul routes to Madurai Branch over Chennai Branch.
    3. Test Inactive Branch Exclusion:
       - When closest branch is `isActive: false`, the next closest active branch is selected.
    4. Test Out-of-State Lead Rejection:
       - "Bangalore" returns `status: 'out_of_state'`, `assignedBranch: null`, `isOutOfState: true`.
    5. Test Unresolvable Lead Rejection:
       - "xyzunknown123" returns `status: 'unresolved'`, `assignedBranch: null`.
    6. Test Audit Logging:
       - Calling `logRoutingActivity` creates a valid record in `prisma.leadActivity`.
  </action>
  <verify>node --env-file=.env --test test/routing.test.mjs</verify>
  <acceptance_criteria>
    - `test/routing.test.mjs` passes all test cases
    - `node --env-file=.env --test test/routing.test.mjs` exits with code 0
  </acceptance_criteria>
  <done>Automated routing test suite built and verified green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --env-file=.env --test test/routing.test.mjs` passes
- [ ] `npx tsc --noEmit` passes
</verification>
