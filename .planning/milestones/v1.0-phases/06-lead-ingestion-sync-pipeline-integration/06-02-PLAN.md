---
phase: 06-lead-ingestion-sync-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/app/api/sheets/external/route.ts
  - src/components/ExternalUploadModal.tsx
  - src/app/api/webhooks/lead/route.ts
  - src/app/dashboard/page.tsx
  - test/ingestion.test.mjs
autonomous: true
requirements:
  - INGEST-02
  - INGEST-03
must_haves:
  - truths:
    - "External Excel/CSV upload auto-assigns branch when branch column is omitted"
    - "ExternalUploadModal mapping hints recognize location/city/zipcode"
    - "Inbound lead webhook automatically routes nearest branch and records audit log"
    - "Dashboard UI renders distinct Unassigned red badge for leads without branch"
    - "Automated tests verify ingestion pipeline routing"
  - artifacts:
    - test/ingestion.test.mjs
  - key_links:
    - "src/app/api/webhooks/lead/route.ts imports routeLeadToBranch and logRoutingActivity"
    - "src/app/api/sheets/external/route.ts imports routeLeadToBranch and logRoutingActivity"
---

<objective>
Update the external upload handler, modal mapping hints, inbound webhook handler, and dashboard unassigned UI badge. Build the end-to-end ingestion test suite (`test/ingestion.test.mjs`).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update External Upload Handler & Modal Hints</name>
  <files>src/app/api/sheets/external/route.ts, src/components/ExternalUploadModal.tsx</files>
  <read_first>src/app/api/sheets/external/route.ts, src/components/ExternalUploadModal.tsx, src/lib/location/routing.ts</read_first>
  <action>
    1. In `src/app/api/sheets/external/route.ts`:
       - Import `routeLeadToBranch`, `logRoutingActivity`, and `BranchCandidate` from `@/lib/location/routing`.
       - Pre-fetch active branches once before the row loop:
         `const activeBranches = await prisma.branch.findMany({ where: { isActive: true } });`
       - In row processing loop, if `!branch && cleanCity`:
         - Call `await routeLeadToBranch(cleanCity, { candidateBranches: activeBranches })`.
         - If assigned, set `branch = routing.assignedBranch.name`.
         - Track routing result to log audit activity after lead creation.
    2. In `src/components/ExternalUploadModal.tsx`:
       - Adjust `CRM_FIELDS`:
         - `branch`: hints `["branch", "showroom", "outlet", "dealer"]` (remove "location").
         - `city`: label `"City / Location / Zipcode"`, hints `["location", "city", "town", "place", "district", "address", "zipcode", "pincode", "postal"]`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/app/api/sheets/external/route.ts` resolves branches for unassigned rows
    - `ExternalUploadModal.tsx` correctly maps location and zipcode to city field
    - `npx tsc --noEmit` exits with 0
  </acceptance_criteria>
  <done>External upload handler and modal mapping hints updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update Webhook Handler & Dashboard UI Badge</name>
  <files>src/app/api/webhooks/lead/route.ts, src/app/dashboard/page.tsx</files>
  <read_first>src/app/api/webhooks/lead/route.ts, src/app/dashboard/page.tsx</read_first>
  <action>
    1. In `src/app/api/webhooks/lead/route.ts`:
       - Import `routeLeadToBranch` and `logRoutingActivity` from `@/lib/location/routing`.
       - If `!branch && (city || body.zipcode || body.location)`:
         - Call `const routing = await routeLeadToBranch(parsedCity || body.zipcode || body.location)`.
         - If `routing.status === 'assigned'` and `routing.assignedBranch`:
           - Use `routing.assignedBranch.name` for `lead.branch`.
       - After `prisma.lead.create`:
         - If routing occurred, call `await logRoutingActivity(lead.id, routing)`.
    2. In `src/app/dashboard/page.tsx`:
       - In desktop table (`<td>` for branch) and mobile view (`<div className="lead-mobile-meta-item">` for branch):
         - If `lead.branch` has a value, render branch pills as before.
         - If `!lead.branch`: render a distinct red badge:
           ```tsx
           <span
             style={{
               background: "rgba(239, 68, 68, 0.1)",
               color: "#dc2626",
               border: "1px solid rgba(239, 68, 68, 0.25)",
               padding: "2px 8px",
               borderRadius: "12px",
               fontSize: "11px",
               fontWeight: 600,
               whiteSpace: "nowrap",
               display: "inline-block",
             }}
             title="Unassigned branch — Out of state or unresolved location"
           >
             Unassigned
           </span>
           ```
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - Webhook auto-routes incoming leads lacking branch and records audit log
    - Dashboard displays red "Unassigned" badge for leads without branch
    - `npx tsc --noEmit` exits with 0
  </acceptance_criteria>
  <done>Webhook auto-routing and dashboard unassigned badge implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Build Automated Ingestion Test Suite</name>
  <files>test/ingestion.test.mjs</files>
  <action>
    Create `test/ingestion.test.mjs`:
    1. Verify Sheet Sync / Ingestion branch resolution without branch column:
       - Row with city "Coimbatore" assigns to nearest Coimbatore branch.
       - Row with city "Madurai" assigns to nearest Madurai branch.
       - Row with city "Bengaluru" (out of state) leaves branch empty string `""`.
    2. Verify Existing Lead Branch Override Protection:
       - If lead in DB already has `branch: "Tata Coimbatore Central"`, an incoming sheet row with `branch: ""` does not clear or alter the lead's branch.
    3. Verify Webhook auto-routing logic.
  </action>
  <verify>node --env-file=.env --test test/ingestion.test.mjs</verify>
  <acceptance_criteria>
    - `test/ingestion.test.mjs` executes and passes all test assertions
    - `node --env-file=.env --test test/ingestion.test.mjs` exits with 0
  </acceptance_criteria>
  <done>Automated ingestion test suite created and passing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` returns 0 errors
- [ ] `node --env-file=.env --test test/ingestion.test.mjs` passes
</verification>
