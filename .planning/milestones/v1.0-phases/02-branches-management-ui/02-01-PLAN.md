---
phase: 02-branches-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/app/branches/page.tsx
autonomous: true
requirements:
  - BRANCH-02
must_haves:
  truths:
    - "Sidebar includes a Branches navigation link for admin users navigating to /branches"
    - "Branches page loads branches from /api/branches?includeInactive=true with loading state"
    - "Branches page displays metric cards for Total, Active, Inactive, and Coordinated branches"
    - "Branches page renders search and status filters that filter branches in real-time"
    - "Branches table displays name, code, city, address, coordinates, radius, and status badge"
  artifacts:
    - src/components/Sidebar.tsx
    - src/app/branches/page.tsx
  key_links:
    - "src/components/Sidebar.tsx links to /branches with active route highlighting"
    - "src/app/branches/page.tsx queries /api/branches?includeInactive=true"
---

<objective>
Integrate the Branches navigation link into `Sidebar.tsx` and build the main Branches management page (`/branches`) with metric summary cards, real-time search and status filtering, responsive branch table, and status badges.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-branches-management-ui/02-CONTEXT.md
@.planning/phases/02-branches-management-ui/02-RESEARCH.md

@src/components/Sidebar.tsx
@src/app/accounts/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Branches Navigation Link to Sidebar</name>
  <files>src/components/Sidebar.tsx</files>
  <read_first>src/components/Sidebar.tsx</read_first>
  <action>
    Update `src/components/Sidebar.tsx`:
    1. In the `{isAdmin && (...) }` navigation section (adjacent to Consultants and Accounts), add the `/branches` Link:
    ```tsx
    <Link
      href="/branches"
      onClick={() => setMobileOpen(false)}
      className={`sidebar-link ${pathname === "/branches" ? "active" : ""}`}
    >
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
        <path d="M3 21h18M3 10h18M5 6l7-3 7 3M4 10v11M20 10v11M8 14v2M12 14v2M16 14v2" />
      </svg>
      Branches
    </Link>
    ```
    2. Ensure proper mobile drawer auto-closing on click.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/components/Sidebar.tsx` contains `href="/branches"`
    - Navigation link is rendered inside `{isAdmin && (...) }`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Branches link added to Sidebar for administrative users with matching styling and icon.</done>
</task>

<task type="auto">
  <name>Task 2: Build Main Branches Management Page</name>
  <files>src/app/branches/page.tsx</files>
  <read_first>src/app/accounts/page.tsx, src/app/globals.css</read_first>
  <action>
    Create `src/app/branches/page.tsx`:
    1. Define `Branch` interface:
       ```typescript
       interface Branch {
         id: number;
         name: string;
         code: string;
         address: string;
         city: string;
         latitude: number | null;
         longitude: number | null;
         radiusKm: number;
         isActive: boolean;
         createdAt: string;
         updatedAt: string;
       }
       ```
    2. Enforce admin access: Check `/api/auth/me` on mount. If user is neither `ADMIN` nor `SUPERADMIN`, display an access denied message with a link back to `/dashboard`.
    3. Implement data fetching from `/api/branches?includeInactive=true`.
    4. Render metric summary cards at the top:
       - **Total Branches**: `branches.length`
       - **Active Branches**: count where `isActive === true`
       - **Inactive Branches**: count where `isActive === false`
       - **GPS Coordinated**: count where `latitude !== null && longitude !== null`
    5. Build filter controls:
       - Search input (debounced or real-time text matching against `name`, `code`, `city`, `address`).
       - Status filter tabs or pill selectors: "All", "Active", "Inactive".
       - "+ Add Branch" primary button with Tata Blue accent (`var(--primary)`).
    6. Build the branch data table / cards:
       - Columns: Status badge, Branch Name & Code, City, Address, GPS Coordinates (displaying `lat, lng` with a quick link to Google Maps if present, or "Not configured" badge), Service Radius, Actions placeholder (Edit & Status toggle).
       - Empty state for when no branches match search or table is empty.
    7. Include Toast notification container for feedback messages.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/app/branches/page.tsx` exists and compiles cleanly
    - Summary stat cards display total, active, inactive, and coordinated counts
    - Search and status filter controls filter list in real-time
    - Table renders branch details with coordinates and status badges
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Branches management page renders metrics, search filter, and branch table.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` returns 0 errors
- [ ] Sidebar renders Branches navigation item
- [ ] `/branches` loads branch data and displays metrics
</verification>
