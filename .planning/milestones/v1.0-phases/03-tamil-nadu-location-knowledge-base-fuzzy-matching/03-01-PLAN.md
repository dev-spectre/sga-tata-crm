---
phase: 03-tamil-nadu-location-knowledge-base-fuzzy-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/location/types.ts
  - src/lib/location/tn-locations.ts
autonomous: true
requirements:
  - LOC-01
must_haves:
  truths:
    - "TypeScript types define LocationNode and LocationMatchResult interfaces"
    - "Embedded dataset covers all 38 Tamil Nadu districts with accurate centroid coordinates"
    - "Knowledge base embeds major urban centers, commercial towns, taluks, and automotive catchments"
    - "Pre-computed index maps support O(1) pincode, exact name, and alias lookups"
  artifacts:
    - src/lib/location/types.ts
    - src/lib/location/tn-locations.ts
  key_links:
    - "src/lib/location/tn-locations.ts exports TN_LOCATIONS array and PINCODE_INDEX, EXACT_INDEX, ALIAS_INDEX"
    - "src/lib/location/types.ts defines LocationNode and LocationMatchResult"
---

<objective>
Construct the authoritative, offline-first Tamil Nadu geographical dataset (`src/lib/location/tn-locations.ts`) and type definitions (`src/lib/location/types.ts`) covering all 38 districts, major towns, taluks, common pincodes, and colloquial aliases.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tamil-nadu-location-knowledge-base-fuzzy-matching/03-CONTEXT.md
@.planning/phases/03-tamil-nadu-location-knowledge-base-fuzzy-matching/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Location Knowledge Base Type Contracts</name>
  <files>src/lib/location/types.ts</files>
  <read_first>.planning/phases/03-tamil-nadu-location-knowledge-base-fuzzy-matching/03-CONTEXT.md</read_first>
  <action>
    Create `src/lib/location/types.ts`:
    1. Define `LocationType`: `'district' | 'city' | 'town' | 'taluk' | 'hub'`.
    2. Define `LocationNode`:
       ```typescript
       export interface LocationNode {
         id: string;
         name: string;
         district: string;
         type: LocationType;
         latitude: number;
         longitude: number;
         pincodes?: string[];
         aliases?: string[];
       }
       ```
    3. Define `LocationMatchResult`:
       ```typescript
       export interface LocationMatchResult {
         matched: boolean;
         query: string;
         canonicalName: string;
         district: string;
         latitude: number;
         longitude: number;
         pincode?: string;
         matchType: 'pincode' | 'exact' | 'alias' | 'substring' | 'fuzzy' | 'none';
         confidence: number;
       }
       ```
    4. Define `MatcherOptions` with configurable minimum confidence threshold (default 0.75).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/lib/location/types.ts` exports `LocationNode`, `LocationMatchResult`, and `MatcherOptions`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Location types and match result contracts defined and type-checked.</done>
</task>

<task type="auto">
  <name>Task 2: Build Comprehensive Tamil Nadu Dataset and Lookup Indexes</name>
  <files>src/lib/location/tn-locations.ts</files>
  <read_first>src/lib/location/types.ts, .planning/phases/03-tamil-nadu-location-knowledge-base-fuzzy-matching/03-RESEARCH.md</read_first>
  <action>
    Create `src/lib/location/tn-locations.ts`:
    1. Populate `TN_LOCATIONS: LocationNode[]` with:
       - All 38 Tamil Nadu districts: Chennai, Coimbatore, Madurai, Tiruchirappalli, Salem, Tiruppur, Erode, Vellore, Tirunelveli, Thanjavur, Dindigul, Kanchipuram, Chengalpattu, Cuddalore, Dharmapuri, Karur, Krishnagiri, Nagapattinam, Namakkal, Nilgiris, Perambalur, Pudukkottai, Ramanathapuram, Ranipet, Sivaganga, Tenkasi, Theni, Thoothukudi, Tirupathur, Tiruvallur, Tiruvannamalai, Tiruvarur, Viluppuram, Virudhunagar, Kallakurichi, Mayiladuthurai, Kanyakumari, Ariyalur.
       - Key commercial and automotive hubs: Avinashi, Palladam, Gobichettipalayam, Sathyamangalam, Bhavani, Perundurai, Tiruchengode, Rasipuram, Attur, Mettur, Sankari, Palani, Kodaikanal, Melur, Thirumangalam, Kovilpatti, Sankarankovil, Ambasamudram, Valliyur, Tambaram, Avadi, Poonamallee, Sriperumbudur, Maraimalai Nagar, Guduvanchery, Sholinganallur, Hosur, Denkanikottai, Vaniyambadi, Ambur, Ooty/Udhagamandalam, Coonoor, Kotagiri, Pollachi, Mettupalayam, etc.
       - Curated colloquial aliases (e.g. `cbe`, `kovai`, `trichy`, `tiruchi`, `mdu`, `madurei`, `ooty`, `nellai`, `mtp`, `hsr`, `slm`, `tpr`, `ngl`, `plni`, `ms`, `mds`, `madras`).
       - Common postal pincodes for major centers (e.g. 641001-641045 for Coimbatore, 600001-600119 for Chennai, 625001-625020 for Madurai, etc.).
    2. Build pre-computed lookup indices for $O(1)$ resolution:
       - `PINCODE_INDEX: Map<string, LocationNode>` mapping 6-digit codes to nodes.
       - `EXACT_INDEX: Map<string, LocationNode>` mapping normalized lowercase canonical names to nodes.
       - `ALIAS_INDEX: Map<string, LocationNode>` mapping normalized lowercase aliases to nodes.
    3. Export `TN_LOCATIONS`, `PINCODE_INDEX`, `EXACT_INDEX`, and `ALIAS_INDEX`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/lib/location/tn-locations.ts` contains all 38 districts and key towns
    - Exports `TN_LOCATIONS`, `PINCODE_INDEX`, `EXACT_INDEX`, and `ALIAS_INDEX`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Embedded Tamil Nadu geographical dataset and O(1) hash maps generated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` returns 0 errors
- [ ] Dataset contains 38 districts and major towns with valid coordinates
- [ ] Pincode and alias hash tables compile without errors
</verification>
